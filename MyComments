
# MY COMMENTS

# deactivate
# python -m venv virt
# virtualenv virt && source virt/Scripts/activate && cd ablog
# virtualenv virt && source virt/Scripts/activate && pip install -r requirements.txt

# python -m venv virt && source virt/Scripts/activate && cd ablog